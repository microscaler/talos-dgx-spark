# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
*.o
*.so
*.dylib
*.dll

# Environment
.env
.env.local

# Temporary files
*.tmp
*.temp
*.log

# Go build artifacts (for Talos installer)
talos/asus-ascent-gx10-overlay/installer/bin/
talos/asus-ascent-gx10-overlay/installer/vendor/
talos/asus-ascent-gx10-overlay/installers/

# Docker image extraction artifacts (from docker save/testing)
talos/asus-ascent-gx10-overlay/blobs/
talos/asus-ascent-gx10-overlay/*.json
talos/asus-ascent-gx10-overlay/oci-layout
talos/asus-ascent-gx10-overlay/repositories
talos/asus-ascent-gx10-overlay/overlay/

# Extracted packages (source .deb files, not needed in repo)
extracted/

# Output directory (build artifacts - deployed via GitHub Actions artifacts, not in repo)
talos/output/*
!talos/output/.gitkeep
# Note: *.tar.gz overlay packages are built artifacts, not tracked in repo
# They are deployed via GitHub Actions artifacts instead

# Python cache in scripts
talos/scripts/__pycache__/
scripts/__pycache__/

# Test artifacts
test-results/
coverage/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

